interface:
  display_name: "Conference Friend Finder"
  short_description: "Find friends' conference posters"
  # This public skill does not bundle private user data.
  # Default active files live in the user's workspace:
  # - ./Friends.md
  # - ./VENUE.md
  # Optional starter templates:
  # - assets/Friends.template.md
  # - assets/VENUE.template.md
  default_prompt: "Use $conference-friend-finder to track conference friends, refresh their affiliations, and find their poster sessions from official venue sources."
